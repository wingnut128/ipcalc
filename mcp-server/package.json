{
  "name": "@ipcalc/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for ipcalc — exposes IPv4/IPv6 subnet calculation tools over stdio",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ipcalc-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --forceExit",
    "lint": "tsc --noEmit"
  },
  "keywords": ["mcp", "ipcalc", "ipv4", "ipv6", "subnet", "calculator"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^22.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
